import styles from './header.module.scss'

export const HeaderComponent = () => {
    return(
        <header>
            <div className={styles.header__logo}>

            </div>
        </header>
    )
}